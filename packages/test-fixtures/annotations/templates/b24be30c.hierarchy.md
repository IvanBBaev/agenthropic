# b24be30c — hand-labeled hierarchy (TEMPLATE — fill me in)

Ground truth for session `b24be30c-2192-407e-82f0-ae0418dc4b7f`, captured from a real Claude Code run.
Anchor session: both on-disk layouts in one directory (20 flat + 22 nested),
15 compaction resets, and genuine depth-2 spawns. 42 subagents.

**What to do:** for every line under `## edges`, replace `__________` with one of

| token | means |
|---|---|
| `ROOT` | this agent was spawned by the main session agent (the top-level conversation) |
| `<hex>` | this agent was spawned by *another subagent* — type that subagent's hex |
| `ORPHAN` | you are sure this transcript records no spawn at all |
| `UNKNOWN` | you cannot tell from the transcript — say this rather than guessing |

`UNKNOWN` is not a failure. A guess that turns out wrong makes the measurement
worse than an honest abstention, because the gate would then be signed against a
label that is not true. Abstentions are reported separately and never counted as
agreement.

**Where to look:** open the main transcript
`spike/corpus/sessions/b24be30c/data/b24be30c-2192-407e-82f0-ae0418dc4b7f.jsonl`
and search for `Task` tool_use blocks — each carries the subagent's
`description`, which is echoed as a comment on each line below. A block found in
the *main* transcript means `ROOT`. A block found inside another agent's
transcript (`b24be30c-2192-407e-82f0-ae0418dc4b7f/subagents/agent-<hex>.jsonl`) means that agent is the parent
— type its hex. Also fill in `labeled-on` with the date you did this.

Everything outside the `## meta` and `## edges` sections is prose and is ignored,
so add notes to yourself freely.

## meta

session: b24be30c-2192-407e-82f0-ae0418dc4b7f
provenance: human
substrate: session-tree:spike/corpus/sessions/b24be30c/data
labeled-by: Ivan Baev
labeled-on: __________
note: labeled by hand from the transcript, independently of the parser output

## edges

a050b34f2248fce5a <- __________   # workflow-subagent:
a0880b1b0f4663a3e <- __________   # workflow-subagent:
a11878d3d183602b8 <- __________   # general-purpose: Cycle-3 architect review Tessera
a17465ba25dcf4c7c <- __________   # Explore: Deep review servicenow-mcp-ai repo
a181570cfe395f6e5 <- __________   # workflow-subagent:
a1f14d131efa98db9 <- __________   # Explore: Verify syncrona repo claims
a20dcba0ef9855dc0 <- __________   # workflow-subagent:
a27a6a097129a3ec2 <- __________   # general-purpose: Dev review cycle 2
a3495289512b92ee7 <- __________   # general-purpose: Cycle-4 QA review Tessera
a355a3014eead9921 <- __________   # general-purpose: Cycle-3 QA review Tessera
a3f07ef63a1738f25 <- __________   # workflow-subagent:
a4052d7b913ed37dc <- __________   # workflow-subagent:
a42e44760607806d2 <- __________   # general-purpose: Architect review cycle 2
a4350c2690813cb6e <- __________   # Explore: Inspect syncrona lock semantics
a4610daed02a5319e <- __________   # general-purpose: Deep research: validate SN assumptions
a4fd4f3edb067930e <- __________   # workflow-subagent:
a564bcdbc253da7c3 <- __________   # general-purpose: QA review cycle 2
a5698d61f116187af <- __________   # workflow-subagent:
a66cff323c01f3a75 <- __________   # workflow-subagent:
a732699890012b308 <- __________   # Explore: DESIGN.md staleness check
a779e75c379d97844 <- __________   # Explore: Verify servicenow-mcp repo claims
a7d388cec84cb581d <- __________   # Explore: Verify syncrona reuse claims
a812f8cc5c84d8a59 <- __________   # Explore: GAP-ANALYSIS.md staleness check
a94d700e372daa327 <- __________   # Explore: Deep review syncrona repo
a9678b3b496e8ba67 <- __________   # workflow-subagent:
a97daf4bf680c76b3 <- __________   # workflow-subagent:
a9bcfeddf52df5d4c <- __________   # workflow-subagent:
aa33753f64e580314 <- __________   # workflow-subagent:
aa690bb28053b127a <- __________   # workflow-subagent:
ab9af4aa8d74a5337 <- __________   # general-purpose: Cycle-4 dev review Tessera
acd8bb4d92d63d6a0 <- __________   # general-purpose: Cycle-4 architect review Tessera
ad0eb571e12c8fe91 <- __________   # workflow-subagent:
ad4f38d48dc308f26 <- __________   # general-purpose: Cycle-3 dev review Tessera
add088580bee8bf9d <- __________   # workflow-subagent:
ae060a5c5242b561e <- __________   # workflow-subagent:
ae0acdbdb85b78a83 <- __________   # workflow-subagent:
ae10369b9e783d69c <- __________   # workflow-subagent:
ae16a6f24078d1486 <- __________   # Explore: Inspect ATF result surface in mcp-ai
ae18ab7e58f087f3f <- __________   # workflow-subagent:
aee3c19d5af2681ee <- __________   # workflow-subagent:
af5fd62566f20edc1 <- __________   # workflow-subagent:
afcc2ef05bf077442 <- __________   # Explore: Verify servicenow-mcp reuse claims
